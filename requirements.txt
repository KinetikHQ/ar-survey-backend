fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
aiosqlite==0.20.0
redis==5.2.1
rq==2.1.0
boto3==1.35.90
python-multipart>=0.0.20
pydantic-settings==2.7.1
python-dotenv==1.0.1
PyJWT>=2.8.0
numpy>=1.24.4

# Testing
pytest>=8.0.0
httpx>=0.27.0

# SAM2 (optional — requires Python >= 3.10, PyTorch >= 2.5.1)
# Install from source:
#   git clone https://github.com/facebookresearch/sam2.git
#   cd sam2 && pip install -e .
# Pipeline falls back gracefully if SAM2 is not installed.
